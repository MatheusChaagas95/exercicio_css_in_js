import styled from 'styled-components'

const ListaVagasWrapper = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 16px;
  padding: 16px;
`
export default ListaVagasWrapper
